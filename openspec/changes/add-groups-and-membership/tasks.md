## 1. Модель данных и доступ

- [x] 1.1 Создать GroupsModule, схемы Group, GroupMembership, GroupMembershipEvent и индексы из design; зарегистрировать модуль, не меняя User и финансовые схемы.
- [x] 1.2 Реализовать GroupsAccessService и протокол транзакционных мутаций через mutationVersion; покрыть проверку действующего членства, владельца, deletedAt и повторную проверку прав после конфликта.
- [x] 1.3 Реализовать безопасные DTO ответов и пагинацию списков; добавить проверки, что email, секреты пользователя и внутренние поля не раскрываются.

## 2. Жизненный цикл группы

- [x] 2.1 Реализовать атомарное создание группы, членства владельца и события; добавить тест отката при ошибке записи.
- [x] 2.2 Реализовать список и получение доступных групп, редактирование названия/описания; покрыть валидацию, неизменяемость типа, пустой PATCH и изоляцию чужой группы.
- [x] 2.3 Реализовать логическое удаление группы с событием; проверить недоступность для всех участников, инвалидность ссылок и сохранность личных данных.

## 3. Членство и владение

- [x] 3.1 Реализовать список действующих участников с разрешёнными полями и вычисляемой ролью.
- [x] 3.2 Реализовать самостоятельный выход и исключение владельцем с событиями; покрыть запрет выхода владельца, чужие идентификаторы, повторное исключение и отзыв доступа со старым JWT.
- [x] 3.3 Реализовать атомарную передачу владения действующему участнику; покрыть передачу себе, отсутствующему/бывшему участнику и отзыв полномочий прежнего владельца.

## 4. Приглашения

- [x] 4.1 Создать схемы и индексы приглашений и счётчиков лимита; реализовать генерацию токена, хранение digest и срок 7 суток с управляемыми часами для тестов.
- [x] 4.2 Реализовать создание, пагинированный список и отзыв приглашений владельцем; проверить отсутствие секрета в списке, состояния expired/accepted/revoked и отзыв новым владельцем.
- [x] 4.3 Реализовать атомарное принятие и повторное вступление с сохранением истории; покрыть существующее членство, повторное использование, истечение ровно в expiresAt, отзыв и удалённую группу.
- [x] 4.4 Реализовать отдельный guard лимита принятия по пользователю с общим MongoDB-счётчиком; проверить 10 запросов за окно, 429/Retry-After, границу окна, конкурентный upsert и отказ при недоступности хранилища.

## 5. HTTP и регрессии

- [x] 5.1 Подключить все маршруты из design с @CurrentUser(), глобальной JWT-защитой, DTO и нормализованными ошибками; проверить методы, HTTP-коды, ObjectId и неизвестные поля.
- [x] 5.2 Добавить end-to-end сценарий семьи и двух организаций: независимые членства, приглашение, передача, выход, исключение, повторное вступление и удаление; подтвердить неизменность личных счетов, операций и отчётов.
- [x] 5.3 Добавить HTTP security tests для всех маршрутов: без JWT, чужая группа, недостаточная роль, старый JWT после исключения/передачи, отсутствие секретов в ответах, ошибках и логах. Проверить учёт невалидных DTO в лимите принятия.
- [x] 5.4 Добавить интеграционные тесты на настоящем MongoDB replica set, не заменяя транзакции моками: одна ссылка/два получателя, две ссылки/один получатель, принятие против отзыва/удаления, передача против выхода/исключения/другой передачи. Использовать управляемую синхронизацию, проверить события и отсутствие частичных изменений.

## 6. Документация и проверка

- [x] 6.1 Документировать повторяемое создание индексов, запуск replica-set тестов, развёртывание и откат без удаления коллекций; добавить пример клиентской ссылки с fragment и принятия через JSON body.
- [x] 6.2 Выполнить затронутые unit/integration tests, затем npm run test, npm run test:e2e и npm run build; зафиксировать точные команды и результаты, включая фактический запуск конкурентных тестов на replica set.
- [x] 6.3 Выполнить openspec validate add-groups-and-membership --strict; сверить реализацию со всеми сценариями трёх спецификаций и подтвердить, что групповые финансовые API остались вне этого change.
