## 1. Зафиксировать общие lifecycle-контракты

- [x] 1.1 Добавить общие типы стратегий `RESTRICT`, `ARCHIVE`, `CASCADE`, `ERASE/ANONYMIZE` и доменные ошибки удаления без Mongoose delete middleware.
- [x] 1.2 Добавить controller-contract тесты для единых ответов DELETE: 204 без тела, 404 для отсутствующей/повторно удалённой сущности и 409 для нарушения `RESTRICT`.
- [x] 1.3 Нормализовать личные DELETE-контроллеры на 204 и безопасные сообщения 404/409, не меняя права доступа и user scoping.

## 2. Расширить модель данных и индексы

- [x] 2.1 Добавить lifecycle-поля счетов, планов и долгов, сериализующий version/lock категории и терминальные причины group membership/invitation с backward-compatible defaults.
- [x] 2.2 Добавить неизменяемую origin metadata финансовых операций для плана и долга и записывать её при закрытии плана и создании дохода погашения.
- [x] 2.3 Добавить состояния пользователя `active`, `deletion_pending`, `deleted`, `deletedAt` и схему durable user-deletion job с уникальностью, этапом, lease, попытками и безопасной ошибкой.
- [x] 2.4 Добавить индексы для `sourceAccountId`, category references, plan expense/origin, debt references, lifecycle filters и user/group cleanup; покрыть определения schema-тестами.

## 3. Создать аудит и миграцию целостности

- [x] 3.1 Реализовать read-only CLI-аудит ссылок account/sourceAccount/snapshot/category/plan expense/debt/user/group с JSON-итогом и ненулевым exit code при нарушениях.
- [x] 3.2 Добавить deterministic fixtures и тесты, доказывающие обнаружение каждого поддерживаемого типа orphan reference и отсутствие мутаций аудитором.
- [x] 3.3 Реализовать идемпотентную миграцию только однозначно исправимых lifecycle/origin данных и отдельный отчёт неоднозначных финансовых нарушений без угадывания сумм.
- [x] 3.4 Задокументировать порядок запуска baseline audit, миграции, повторного audit и критерий остановки rollout.

## 4. Устранить опасное удаление счетов

- [x] 4.1 Добавить regression-тесты удаления balance/saving, когда счёт встречается в `accountId` и `sourceAccountId`, включая проверку неизменности обоих балансов и snapshots при 409.
- [x] 4.2 Заменить личный bulk-cascade операций на атомарный `RESTRICT`, проверяющий начальный остаток и всю историю по `accountId`/`sourceAccountId`; удалять каскадом только snapshots пустого счёта.
- [x] 4.3 Добавить явное архивирование личного счёта, исключение архивных счетов из новых операций и сохранение исторического чтения/отчётности.
- [x] 4.4 Унифицировать личное и групповое удаление пустого счёта и добавить concurrency-тесты удаления против создания дохода, расхода и перевода.

## 5. Сделать удаление операций единообразным

- [x] 5.1 Добавить personal transaction regression-тесты soft delete для income, expense и save, включая повторное и конкурентное удаление и компенсацию всех затронутых счетов ровно один раз.
- [x] 5.2 Перевести личные транзакции с `findOneAndDelete` на compare-and-set `deletedAt/deletedBy` внутри существующей account-lock транзакции.
- [x] 5.3 Централизовать active-transaction filter и применить его ко всем личным спискам, суммам, лимитам, reports и поиску одной операции; dependency checks должны намеренно видеть удалённую историю.
- [x] 5.4 Добавить интеграционные тесты, подтверждающие, что удалённые личные и групповые операции не попадают в обычные результаты, но продолжают блокировать удаление справочников и счетов.

## 6. Атомаризировать категории и leaf deletion

- [x] 6.1 Добавить unit/integration-тесты, что личную категорию нельзя удалить при ссылке из активной или удалённой операции, лимита либо плана.
- [x] 6.2 Ввести общий сериализующий category lock для создания/переназначения расходов, лимитов и планов и для проверки с последующим удалением категории в одной MongoDB transaction.
- [x] 6.3 Согласовать personal/group category `RESTRICT + ARCHIVE`, запретить новые назначения архивных категорий и сохранить изменение старых записей без смены категории.
- [x] 6.4 Подтвердить тестами физическое удаление лимита без изменений категории и финансовой истории и единый ответ 204.
- [x] 6.5 Добавить replica-set concurrency-тесты удаления/архивирования категории против создания расхода, лимита и плана.

## 7. Сохранить историю планов и долгов

- [x] 7.1 Добавить lifecycle-тесты: физическое удаление допускается только для активного плана без расхода и долга без repayments/generated income.
- [x] 7.2 Реализовать `RESTRICT` для планов/долгов с историей и явные archive actions, исключающие архивные записи из обычных списков без удаления связанной истории.
- [x] 7.3 Сохранять и проверять origin metadata при закрытии плана и создающем income погашении, не каскадируя удаление финансовой операции из источника.
- [x] 7.4 Добавить тесты поведения при удалении origin-транзакции, архивировании источника и конкурентных close/repay/archive запросах.

## 8. Завершать дочерние состояния удалённой группы

- [x] 8.1 Расширить удаление группы: одним timestamp атомарно установить `deletedAt`, завершить active memberships с причиной `group_deleted` и отозвать pending invitations с той же причиной.
- [x] 8.2 Сохранить group finances, membership events и терминальные invitations и подтвердить, что все публичные маршруты удалённой группы остаются недоступны.
- [x] 8.3 Обновить существующие replica-set concurrency-тесты удаления группы против accept/leave/remove/transfer и проверить отсутствие active memberships и pending invitations после commit.

## 9. Реализовать удаление пользователя

- [x] 9.1 Добавить защищённую команду запроса удаления аккаунта с password/second-factor step-up, проверкой владения неудалённой группой и ответами 202/409.
- [x] 9.2 В одной транзакции переводить пользователя в `deletion_pending`, увеличивать `authVersion` и upsert-ить единственную deletion job; auth/login/guards должны принимать только `active` пользователя.
- [x] 9.3 Реализовать Mongo-backed worker с атомарной арендой и идемпотентными этапами завершения memberships и очистки личных финансовых коллекций в безопасном порядке.
- [x] 9.4 Добавить этап очистки auth challenges и 2FA, удаления IP/User-Agent из security audit и превращения User в PII-free tombstone со статусом `deleted`.
- [x] 9.5 Добавить тесты повтора после сбоя на каждом этапе, конкуренции двух workers, немедленного отзыва JWT, блокировки login и сохранения обезличенной групповой истории.
- [x] 9.6 Добавить отключаемый runtime rollout-флаг для приёма новых deletion jobs и dry-run диагностику cleanup до production enablement.

## 10. Документация и итоговая проверка

- [x] 10.1 Обновить API-документацию матрицей удаления, archive actions, ответами 202/204/404/409, необратимостью user deletion и эксплуатационным порядком rollout/rollback.
- [x] 10.2 Запустить узкие unit/integration тесты каждого изменённого домена и real-replica-set concurrency suites для счетов, категорий, групп и user-deletion worker.
- [x] 10.3 Запустить `npm run test`, `npm run test:e2e`, `npm run build` и strict OpenSpec validation, зафиксировав команды и результаты.
- [x] 10.4 Выполнить финальный integrity audit на мигрированной тестовой базе и подтвердить отсутствие известных orphan references.
