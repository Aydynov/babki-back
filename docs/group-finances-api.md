# Групповые финансы

Все маршруты ниже относительны к `/api/v1/groups/:groupId` и требуют JWT действующего участника. Семья и организация имеют одинаковые финансовые правила. Личные маршруты остаются личными; групповые операции не входят в личные расходы автора или указанного участника.

## Права

Владелец имеет все права. У нового/повторно вступившего участника все три финансовых права выключены. Владелец выдаёт непустое подмножество прав через `PATCH /members/:userId/permissions`, например:

```json
{"manageCategories":true,"manageLimits":false}
```

| Действие | Право |
| --- | --- |
| Создать/удалить неиспользованный кошелёк | manageAccounts |
| Создать/изменить/архивировать/удалить категорию | manageCategories |
| Создать/изменить/удалить лимит | manageLimits |
| Читать финансовые данные, вносить доходы и расходы | Любой действующий участник |
| Изменить/удалить расход | Владелец, действующий автор либо участник расхода |
| Изменить участника расхода | Владелец либо действующий автор |
| Изменить/удалить доход | Владелец либо действующий автор |
| Выдать права, управлять группой и приглашениями | Только владелец |

Права настроек не дают дополнительных прав на чужие операции. Выход/исключение сбрасывает права, повторное вступление их не восстанавливает. При передаче владения прежний владелец становится участником без делегированных прав. Проверки используют текущее состояние базы, а не сведения в JWT.

## Кошелёк

`POST /accounts` создаёт один основной кошелёк группы, например `{"amount":1000,"openedAt":"2024-01-01T00:00:00Z"}`. Начальная сумма неотрицательна, по умолчанию 0; дата открытия по умолчанию равна текущему времени. Создание группы само по себе кошелёк не создаёт. Повторное создание — 409.

`GET /accounts` возвращает список, `GET /accounts/:accountId` — счёт с текущим amount и последними месячными снимками timeline. До даты открытия баланс равен нулю. Снимки — кэш остатков на конец месяца UTC; в текущем месяце текущий amount может отличаться от снимка при будущих операциях.

`DELETE /accounts/:accountId` допустим только при нулевом начальном остатке и отсутствии истории операций, включая удалённые. Корректировка баланса выполняется доходом/расходом; прямого редактирования снимков группы нет. Групповые накопления и переводы между бюджетами в этот этап не входят.

## Операции

```json
{
  "accountId":"<счёт группы>",
  "categoryId":"<категория группы>",
  "participantId":"<действующий участник>",
  "amount":150.50,
  "transactionDate":"2024-02-10T12:00:00Z",
  "description":"Продукты"
}
```

`POST /expenses` принимает этот объект. participantId необязателен, по умолчанию вызывающий пользователь. createdBy сервер назначает из JWT. Дополнительно поддерживаются merchant и items. Расход может быть внесён за другого участника. В ответе сохраняются createdBy и participantId.

`POST /incomes` принимает accountId, amount, transactionDate и необязательное description. У дохода нет participantId. Суммы положительны с точностью до двух десятичных знаков. Дата операции не может быть раньше открытия кошелька. Расход может привести к отрицательному учётному остатку.

`GET /expenses`, `GET /incomes`, `GET /transactions` возвращают `{items,total,page,limit}`. Поддерживаются accountId, fromDate, toDate, transactionType; у расходов дополнительно categoryId и participantId. `GET /expenses/revenue` возвращает totalRevenue с теми же фильтрами расходов. Фильтр participantId включает исторические расходы бывших участников и не означает фильтр автора.

`GET /expenses/:id`, `PATCH /expenses/:id`, `DELETE /expenses/:id` и аналогичные пути incomes работают с одной записью. PATCH расхода: amount, description, merchant, items, categoryId, participantId. PATCH дохода: amount, description. Счёт, автор и дата неизменяемы. При изменении иных полей разрешено сохранить прежнего бывшего участника или архивную категорию; новое назначение на них запрещено. Удаление логическое, повторное удаление — 404; компенсация баланса выполняется один раз.

## Категории, лимиты, отчёты

- `/expense-categories`: GET и POST; `/:id`: GET, PATCH, DELETE. Поля name (1–100 после trim), description, color; PATCH также принимает isArchived. Имя уникально внутри бюджета. Категорию со ссылками из операций/лимитов удалить нельзя; архивирование сохраняет историю, запрещая новые назначения.
- `/expense-limits`: GET и POST; `/:id`: GET, PATCH, DELETE. Создание принимает categoryId, total, startDate/endDate (по умолчанию текущий месяц UTC). PATCH принимает total/startDate/endDate. GET списка поддерживает categoryId/periodDate. Пересекающиеся включительные периоды одной категории — 409. rest учитывает всех участников и может стать отрицательным; лимит не блокирует расходы.
- `/reports/months`, `/reports/years`: GET, fromDate/toDate и categories (массив либо строка ObjectId через запятую). Возвращают period, expenses, incomes, saves=0, saving=0, balance и expensesByCategory. Категории фильтруют расходы; balance остаётся остатком общего кошелька. participantId не поддерживается в отчёте с общим балансом — используйте список/сумму расходов.

Временные границы групповых отчётов и лимитов используют UTC. Выход участника не меняет историю/суммы. После удаления группы её финансовые данные сохраняются, но все соответствующие маршруты становятся недоступными.

## Ошибки и эксплуатация

Создание возвращает 201, чтение/изменение — 200, групповое удаление — 204. Некорректные/неизвестные поля и пустой PATCH — 400; без JWT — 401; недостаточно прав действующего участника — 403; недоступная группа/объект — 404; конфликт состояния — 409. Сбой финансовой транзакции — 503. Изменения записи и баланса атомарны, в том числе при конкуренции с отзывом прав или исключением.

Развёртывание требует MongoDB replica set. Промышленная база для этого релиза считается пустой: перенос legacy-данных и startup-проверка миграции не предусмотрены. Mongoose создаёт индексы из актуальных схем. Для локальной ручной проверки используйте [development seed](group-finances-seed.md).

Проверки: `npm run test`, `npm run test:e2e -- --runInBand`, `npm run build`. E2E используют временный локальный mongod replica set; путь к бинарнику можно задать MONGOD_BINARY. Тесты не подключаются к рабочей базе.
